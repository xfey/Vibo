# main/services/agents/opencode 索引

职责：

* 管理 `OpenCode CLI` 可用性检查，并构建 `OpenCode` 的启动参数

当前目录文件：

* `README.md`
  * 当前目录索引
* `opencode-agent-adapter.ts`
  * 负责 OpenCode CLI 可用性检查，构建 `New OpenCode / Resume OpenCode` 的 `LaunchSpec`
  * 核心导出：`OpenCodeAgentAdapter`
