# main/services/agents/codex 索引

职责：

* 管理 `Codex CLI` 可用性检查，并构建 `Codex` 的启动参数

当前目录文件：

* `README.md`
  * 当前目录索引
* `codex-agent-adapter.ts`
  * 负责 Codex CLI 可用性检查，构建 `New Codex / Resume Codex` 的 `LaunchSpec`
  * 核心导出：`CodexAgentAdapter`
