# renderer/features 索引

职责：

* 按产品功能拆分界面模块

当前目录文件：

* `README.md`
  * 当前目录索引

当前子目录：

* `launcher/`
  * 启动页、最近项目与 recent agent badge
* `project-home/`
  * 项目进入后的首页、会话入口与带品牌标识的项目级设置
* `terminals/`
  * terminal UI、带 agent 角标的 tabs 与 workspace 编排
* `hub/`
  * Hub 文件树、底部 Git 提交列表、右键文件操作、文件图标主题与文件预览 / 编辑区
* `settings/`
  * `App Settings` 覆盖层，以及 `Theme Presets / Font / Agents / Skills` 分节组件
